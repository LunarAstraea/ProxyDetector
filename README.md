To run the bot:
1. Change "guild_id = " in main.py to the ID of your server, the bot currently does not allow running on multiple servers.
2. Put a Bot Token into TOKEN= in .env
3. Install requirements in requirements.txt
4. Run main.py

Tested on Python version 3.12