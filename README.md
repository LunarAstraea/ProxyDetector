To run the bot:
1. Change "guild_id = <id of your server>" in main.py, the bot currently does not allow running on multiple servers.
2. Put a Bot Token into TOKEN=<your token> in .env
3. Install requirements in requirements.txt
4. Run main.py

Tested on Python version 3.12